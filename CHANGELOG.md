# 모루 변경 기록

이 문서는 개인용 뽀모도로 앱 **모루**에 추가된 기능과 관련 파일을 정리한다.

## 2026-08-30: 초기 앱 구현

### 추가된 기능

- 집중, 짧은 휴식, 긴 휴식 타이머
- 타이머 시작·일시 정지·초기화·다음 모드 이동
- 스페이스바를 이용한 시작 및 일시 정지
- 오늘 할 일 추가, 선택, 완료 표시, 삭제
- 선택한 할 일과 완료한 집중 세션 연결
- 완료한 세션의 작업명, 집중 시간, 완료 시각 표시
- 오늘의 누적 집중 시간과 세션 횟수 표시
- 할 일과 집중 기록을 브라우저 `localStorage`에 저장
- 데스크톱과 모바일 화면에 대응하는 반응형 레이아웃

### 변경된 파일과 코드

#### `app/page.tsx`

- `Mode`, `Task`, `Session` 타입으로 타이머 모드, 할 일, 집중 기록의 데이터 구조를 정의했다.
- `setInterval`을 사용하는 타이머 로직과 모드별 시간 초기화 로직을 추가했다.
- `moru-tasks`, `moru-sessions` 키를 사용해 할 일과 세션 기록을 저장하고 다시 불러오도록 구현했다.
- 할 일 입력 폼과 완료·선택·삭제 버튼을 추가했다.
- 집중 세션이 끝나면 현재 선택한 할 일과 완료 시각을 기록하도록 구현했다.
- 스페이스바 입력을 감지해 타이머를 시작하거나 멈추도록 처리했다.

#### `app/globals.css`

- 종이 플래너를 연상시키는 베이지 배경, 붉은 강조색, 카드형 레이아웃을 적용했다.
- 타이머, 모드 탭, 할 일 목록, 집중 기록의 상태별 스타일을 추가했다.
- 화면 너비 850px과 450px을 기준으로 모바일 레이아웃을 조정했다.
- 사용자가 동작 줄이기를 설정한 경우를 고려해 애니메이션 조건을 지정했다.

#### `app/layout.tsx`

- 페이지 언어를 한국어(`ko`)로 설정했다.
- 사이트 제목을 `모루 — 나만의 집중 타이머`로 변경했다.
- 개인용 뽀모도로 앱을 설명하는 메타 설명을 추가했다.

#### `.openai/hosting.json`

- 비공개 Sites 배포에 사용하는 프로젝트 식별 정보를 추가했다.
- 타이머 데이터와 할 일 데이터는 배포 서버가 아닌 각 브라우저에 저장된다.

## 2026-08-30: 집중 시간 직접 설정

### 변경된 기능

- 고정된 25분 집중 시간 대신 1~120분 사이의 시간을 사용할 수 있다.
- `−`, `+` 버튼으로 집중 시간을 5분씩 조절할 수 있다.
- 숫자 입력란에서 원하는 시간을 직접 입력할 수 있다.
- 타이머가 실행 중일 때는 시간 설정을 잠가 진행 중인 세션이 바뀌지 않도록 했다.
- 설정한 집중 시간은 브라우저를 닫았다가 다시 열어도 유지된다.
- 변경한 시간으로 세션을 완료하면 실제 설정값이 오늘의 기록에 저장된다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `focusDuration` 상태를 추가하고 기본값을 25분으로 지정했다.
- `changeFocusDuration` 함수에서 입력값을 1~120분 범위로 제한한다.
- `moru-focus-duration` 키를 사용해 설정값을 `localStorage`에 저장하고 불러온다.
- 집중 모드 전환, 초기화, 세션 완료 기록이 `focusDuration` 값을 사용하도록 변경했다.
- 사이드 패널에 `duration-card` 영역과 시간 감소·직접 입력·증가 컨트롤을 추가했다.

#### `app/globals.css`

- `duration-card`, `duration-control` 스타일을 추가했다.
- 실행 중 비활성화된 버튼과 입력란의 상태가 구분되도록 스타일을 지정했다.

## 확인 결과

- `npm run build`가 오류 없이 완료됐다.
- 로컬 개발 주소 `http://localhost:3000`에서 정상 응답을 확인했다.

## 데이터 저장 방식

현재 앱은 외부 API나 데이터베이스를 사용하지 않는다. 아래 값은 앱을 연 브라우저의 `localStorage`에만 저장된다.

- `moru-tasks`: 할 일 목록
- `moru-sessions`: 완료한 집중 세션
- `moru-focus-duration`: 사용자가 설정한 집중 시간

브라우저 데이터를 삭제하거나 다른 브라우저·기기를 사용하면 기존 기록이 나타나지 않는다.

## 2026-08-30: 휴식 모드 통합 및 시간 설정

### 변경된 기능

- `짧은 휴식`과 `긴 휴식`을 하나의 `휴식` 모드로 통합했다.
- 휴식 시간의 기본값을 10분으로 변경했다.
- 휴식 시간을 1~120분 사이에서 직접 입력할 수 있다.
- `−`, `+` 버튼으로 휴식 시간을 5분씩 조절할 수 있다.
- 설정한 휴식 시간은 브라우저를 닫았다가 다시 열어도 유지된다.
- 휴식 모드에서는 현재 할 일 대신 화면에서 잠시 눈을 떼라는 안내가 표시된다.
- 휴식 모드의 시작 버튼 문구를 `휴식 시작`으로 구분했다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `Mode` 타입을 `focus | rest`로 단순화하고 `modes`에서 짧은 휴식과 긴 휴식을 제거했다.
- `restDuration` 상태와 `changeRestDuration` 함수를 추가했다.
- `moru-rest-duration` 키를 사용해 휴식 설정을 `localStorage`에 저장하고 불러온다.
- 모드 전환, 초기화, 세션 종료 시 설정된 휴식 시간이 사용되도록 타이머 계산을 변경했다.
- 시간 설정 카드에 집중 시간과 휴식 시간을 각각 조절하는 행을 배치했다.

#### `app/globals.css`

- `duration-card`를 세로형 설정 카드로 변경했다.
- 집중 설정과 휴식 설정을 구분하는 `duration-row` 스타일을 추가했다.

### 데이터 저장 항목 추가

- `moru-rest-duration`: 사용자가 설정한 휴식 시간

## 2026-08-30: 백색소음 기능

### 추가된 기능

- 별도의 음원 파일이나 외부 API 없이 브라우저에서 소음을 직접 생성한다.
- 화이트, 핑크, 브라운 노이즈 중 하나를 선택할 수 있다.
- 백색소음을 개별적으로 켜고 끌 수 있다.
- 볼륨을 0~100% 범위에서 조절할 수 있다.
- 자동 연동을 사용하면 집중 시작 시 재생되고 정지 또는 휴식 전환 시 꺼진다.
- 선택한 소음 종류, 볼륨, 타이머 연동 설정이 브라우저에 저장된다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `NoiseType` 타입과 소음 종류, 볼륨, 재생 상태, 자동 연동 상태를 관리하는 상태값을 추가했다.
- Web Audio API의 `AudioContext`와 반복 재생되는 `AudioBufferSourceNode`를 이용해 소음을 생성한다.
- 화이트 노이즈는 무작위 샘플, 핑크 노이즈는 필터 계산, 브라운 노이즈는 누적 방식으로 서로 다른 음색을 만든다.
- 소음 설정을 `localStorage`에서 불러오고 저장한다.
- 사이드 패널에 재생 버튼, 소음 선택 버튼, 볼륨 슬라이더, 타이머 연동 체크박스를 추가했다.

#### `app/noise.css`

- 백색소음 카드와 재생 상태, 종류 선택, 볼륨 및 자동 연동 컨트롤의 스타일을 추가했다.

#### `app/globals.css`

- 백색소음 전용 스타일 파일인 `noise.css`를 불러오도록 변경했다.

### 데이터 저장 항목 추가

- `moru-noise-type`: 선택한 소음 종류
- `moru-noise-volume`: 설정한 볼륨
- `moru-auto-noise`: 집중 타이머 자동 연동 여부

## 2026-08-30: 환경음 4종 추가

### 추가된 기능

- 기존 화이트, 핑크, 브라운 노이즈에 비행기, 도서관, 빗소리, 카페 환경음을 추가했다.
- 환경음도 기존 재생 버튼, 볼륨 조절, 집중 타이머 자동 연동 설정을 공유한다.
- 환경음이 끝나면 끊김 없이 다시 재생되도록 반복 재생을 적용했다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `NoiseType`을 `SoundType`으로 확장하고 환경음 4종을 추가했다.
- 계산으로 생성하는 노이즈와 실제 음원 파일을 구분해 재생하도록 Web Audio 및 `HTMLAudioElement` 로직을 함께 구성했다.
- 저장된 환경음 선택값도 다음 접속 때 복원하도록 허용 목록을 확장했다.

#### `app/noise.css`

- 일곱 가지 사운드 선택 버튼이 들어가도록 선택 영역을 4열로 변경했다.

#### `public/audio/*`

- `airplane.mp3`, `library.wav`, `rain.m4a`, `cafe.mp3`를 앱용 환경음으로 추가했다.
- 약 160MB였던 도서관 원본은 모노 22.05kHz WAV로 변환해 약 24MB로 줄였다.

#### `AUDIO_CREDITS.md`

- 각 음원의 제작자, 원본 페이지, CC0 라이선스 정보를 기록했다.

#### `.gitignore`

- 원본과 앱용 사본이 중복으로 커밋되지 않도록 로컬 `MP3` 폴더를 제외했다.

## 2026-08-30: 환경음 파일 교체

### 변경된 기능

- 빗소리를 목록에서 제거하고 피아노와 바람 환경음을 추가했다.
- 집중 사운드는 기존 7종에서 화이트, 핑크, 브라운, 비행기, 도서관, 카페, 피아노, 바람의 8종으로 변경됐다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `SoundType`과 저장값 허용 목록에서 `rain`을 제거하고 `piano`, `wind`를 추가했다.
- 피아노와 바람 음원 경로를 실제 파일 재생 목록에 연결했다.
- 화면의 사운드 선택 버튼을 변경된 8종에 맞게 수정했다.

#### `public/audio/*`

- 피아노 원본은 스테레오 22.05kHz WAV로 변환해 약 11MB로 줄였다.
- 바람 원본은 바이노럴 효과를 유지하기 위해 스테레오 44.1kHz WAV로 변환해 약 35MB로 줄였다.

#### `AUDIO_CREDITS.md`

- 제거된 빗소리 항목을 정리하고 피아노와 바람 음원의 제작자·원본 링크를 추가했다.
- 바람 음원의 CC BY 4.0 출처 표시 조건과 확인이 필요한 피아노 라이선스를 기록했다.

## 2026-08-30: 라이선스 미확인 음원 제거

### 변경된 기능

- 라이선스를 확인하지 못한 피아노 음원을 집중 사운드 목록에서 제거했다.
- 집중 사운드는 화이트, 핑크, 브라운, 비행기, 도서관, 카페, 바람의 7종으로 변경됐다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `SoundType`, 저장값 허용 목록, 실제 파일 경로 및 선택 버튼에서 `piano`를 제거했다.

#### `public/audio/piano.wav`

- 앱용으로 변환했던 피아노 음원 파일을 삭제했다.

#### `MP3/866775__josefpres__piano-loops-213-octave-long-loop-120-bpm.wav`

- 라이선스를 확인하지 못한 피아노 원본 파일을 삭제했다.

#### `AUDIO_CREDITS.md`

- 피아노 출처와 라이선스 확인 안내를 제거했다.

## 2026-08-30: 타이머 종료 알림음 및 앱 내 출처 표시

### 추가된 기능

- 집중 또는 휴식 시간이 끝나면 두 음으로 구성된 짧은 종소리가 재생된다.
- 종료 알림음을 켜고 끄거나 별도 볼륨을 조절할 수 있다.
- 타이머를 기다리지 않고 `미리 듣기` 버튼으로 알림음을 확인할 수 있다.
- 알림음 설정은 브라우저에 저장된다.
- 앱 하단에서 환경음 제작자와 바람 음원의 CC BY 4.0 라이선스를 확인할 수 있다.

### 변경된 파일과 코드

#### `app/page.tsx`

- Web Audio API의 사인파 두 개로 외부 파일이 필요 없는 `playChime` 종료음을 구현했다.
- `chimeEnabled`, `chimeVolume` 상태와 저장 로직을 추가했다.
- 타이머가 0초가 될 때 설정에 따라 알림음을 재생하도록 변경했다.
- 집중 사운드 카드에 알림음 설정과 미리 듣기 버튼을 추가했다.
- 하단에 환경음 제작자와 라이선스 정보를 표시했다.

#### `app/noise.css`

- 알림음 설정과 하단 음원 출처 영역의 스타일을 추가했다.

### 데이터 저장 항목 추가

- `moru-chime-enabled`: 종료 알림음 사용 여부
- `moru-chime-volume`: 종료 알림음 볼륨

## 2026-08-30: 배포용 환경음 용량 조정

- 비공개 배포 저장소의 전송 용량에 맞추기 위해 도서관 음원을 모노 11.025kHz, 바람 음원을 스테레오 22.05kHz WAV로 다시 변환했다.
- 도서관 파일은 약 24MB에서 12MB, 바람 파일은 약 35MB에서 18MB로 줄었다.
- 비행기와 카페 음원의 원본 MP3 품질은 유지했다.

## 2026-08-30: 환경음 배포 크기 최종 조정

- 비공개 배포 저장소의 파일 전송 한도에 맞추기 위해 도서관과 바람 음원의 앞부분 2분을 모노 11.025kHz WAV로 변환했다.
- 두 음원은 각각 약 2.5MB이며 앱에서 반복 재생된다.
- 로컬 `MP3` 폴더의 원본 파일은 변경하지 않았다.

## 2026-08-30: 도서관·바람 음원 재생 오류 수정

### 수정된 문제

- 도서관과 바람 음원의 용량을 줄이는 과정에서 WAV 데이터 위치를 44바이트로 고정해 계산한 탓에, 실제 오디오가 없는 0초 파일로 인식되는 문제를 확인했다.
- WAV의 `fmt`와 `data` 청크를 직접 찾아 실제 PCM 데이터를 추출하고 표준 44바이트 헤더를 새로 생성하도록 변환 방식을 수정했다.
- 두 파일 모두 모노 11.025kHz, 16비트 PCM, 재생 시간 120초로 정상 인식되는 것을 확인했다.

### 변경된 파일

- `public/audio/library.wav`
- `public/audio/wind.wav`

## 2026-08-31: 완료한 할 일 자동 정렬

### 변경된 기능

- 오늘 할 일에서 완료 체크한 항목이 미완료 항목 아래, 목록의 맨 밑으로 자동 이동한다.
- 미완료 항목끼리와 완료 항목끼리는 작성된 순서를 유지한다.
- 완료 표시를 해제하면 해당 항목은 다시 미완료 목록으로 이동한다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `orderedTasks`를 추가해 미완료 항목과 완료 항목을 안정적으로 나눠 표시한다.
- 저장된 `tasks` 배열 자체는 변경하지 않아 기존 할 일 데이터와 작성 순서를 보존한다.

## 2026-08-31: 오늘 할 일 날짜별 초기화

### 추가된 기능

- 마지막으로 할 일을 저장한 날짜와 현재 로컬 날짜가 다르면 오늘 할 일을 자동으로 비운다.
- 앱을 다음 날 새로 열었을 때뿐 아니라 자정을 넘겨 계속 실행하거나 다시 화면으로 돌아온 경우에도 날짜 변경을 확인한다.
- 날짜가 바뀌면 선택 중인 할 일도 함께 해제한다.
- 집중 기록, 타이머 시간, 집중 사운드 설정은 초기화하지 않는다.

### 변경된 파일과 코드

#### `app/page.tsx`

- 현재 기기의 로컬 날짜를 `YYYY-MM-DD`로 만드는 `getLocalDateKey` 함수를 추가했다.
- `moru-task-date`에 할 일 저장 날짜를 기록하고 앱 시작 시 현재 날짜와 비교한다.
- 기능 추가 전부터 저장돼 있던 할 일에는 오늘 날짜를 처음 한 번 지정해 기존 목록이 갑자기 사라지지 않게 했다.
- 1분 간격과 브라우저 포커스 복귀 시 날짜를 다시 확인해 새 날이면 할 일 목록을 초기화한다.

### 데이터 저장 항목 추가

- `moru-task-date`: 오늘 할 일이 저장된 로컬 날짜

## 2026-08-31: 날짜 자동 초기화 제거 및 수동 리셋

### 변경된 기능

- 자정을 넘겨 공부할 때 할 일이 갑자기 사라질 수 있어 날짜별 자동 초기화 기능을 제거했다.
- 오늘 할 일 카드 상단에 사용자가 원할 때 누르는 `리셋` 버튼을 추가했다.
- 실수로 전체 목록을 지우지 않도록 확인창에서 한 번 더 묻는다.
- 리셋하면 할 일 목록과 선택 항목만 지워지고 집중 기록, 타이머 시간, 집중 사운드 설정은 유지된다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `getLocalDateKey`, `taskDate`, 날짜 비교 및 주기적 초기화 로직을 제거했다.
- 확인 후 `tasks`와 `selectedTask`를 비우는 `resetTasks` 함수를 추가했다.
- 오늘 할 일 헤더에 목록이 비었을 때 비활성화되는 리셋 버튼을 배치했다.

#### `app/globals.css`

- 할 일 완료 개수와 리셋 버튼을 나란히 배치하는 `task-meta`, `task-reset` 스타일을 추가했다.

### 제거된 데이터 저장 항목

- `moru-task-date`는 더 이상 생성하거나 사용하지 않는다.

## 2026-08-31: 오늘 집중 시간 실시간 표시

### 변경된 기능

- 오른쪽 상단의 오늘 집중 시간에 완료한 세션뿐 아니라 현재 진행 중인 집중 시간도 합산한다.
- 분과 초를 함께 표시해 집중 타이머가 흐를 때 매초 값이 바뀐다.
- 타이머를 잠시 멈춰도 현재까지 집중한 시간은 표시하고, 미완료 세션을 초기화하면 해당 시간은 합계에서 제외한다.
- 휴식 시간은 오늘 집중 시간에 포함하지 않는다.

### 변경된 파일과 코드

#### `app/page.tsx`

- 완료 세션의 분 단위 합계였던 `focusMinutes`를 초 단위 `focusSeconds` 계산으로 변경했다.
- 완료된 집중 시간과 현재 세션에서 경과한 시간을 합산해 상단에 `분 초` 형식으로 표시한다.
