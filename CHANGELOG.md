# 모루 변경 기록

이 문서는 개인용 뽀모도로 앱 **모루**에 추가된 기능과 관련 파일을 정리한다.

## 2026-08-30: 초기 앱 구현

### 추가된 기능

- 집중, 짧은 휴식, 긴 휴식 타이머
- 타이머 시작·일시 정지·초기화·다음 모드 이동
- 스페이스바를 이용한 시작 및 일시 정지
- 오늘 할 일 추가, 선택, 완료 표시, 삭제
- 선택한 할 일과 완료한 집중 세션 연결
- 완료한 세션의 작업명, 집중 시간, 완료 시각 표시
- 오늘의 누적 집중 시간과 세션 횟수 표시
- 할 일과 집중 기록을 브라우저 `localStorage`에 저장
- 데스크톱과 모바일 화면에 대응하는 반응형 레이아웃

### 변경된 파일과 코드

#### `app/page.tsx`

- `Mode`, `Task`, `Session` 타입으로 타이머 모드, 할 일, 집중 기록의 데이터 구조를 정의했다.
- `setInterval`을 사용하는 타이머 로직과 모드별 시간 초기화 로직을 추가했다.
- `moru-tasks`, `moru-sessions` 키를 사용해 할 일과 세션 기록을 저장하고 다시 불러오도록 구현했다.
- 할 일 입력 폼과 완료·선택·삭제 버튼을 추가했다.
- 집중 세션이 끝나면 현재 선택한 할 일과 완료 시각을 기록하도록 구현했다.
- 스페이스바 입력을 감지해 타이머를 시작하거나 멈추도록 처리했다.

#### `app/globals.css`

- 종이 플래너를 연상시키는 베이지 배경, 붉은 강조색, 카드형 레이아웃을 적용했다.
- 타이머, 모드 탭, 할 일 목록, 집중 기록의 상태별 스타일을 추가했다.
- 화면 너비 850px과 450px을 기준으로 모바일 레이아웃을 조정했다.
- 사용자가 동작 줄이기를 설정한 경우를 고려해 애니메이션 조건을 지정했다.

#### `app/layout.tsx`

- 페이지 언어를 한국어(`ko`)로 설정했다.
- 사이트 제목을 `모루 — 나만의 집중 타이머`로 변경했다.
- 개인용 뽀모도로 앱을 설명하는 메타 설명을 추가했다.

#### `.openai/hosting.json`

- 비공개 Sites 배포에 사용하는 프로젝트 식별 정보를 추가했다.
- 타이머 데이터와 할 일 데이터는 배포 서버가 아닌 각 브라우저에 저장된다.

## 2026-08-30: 집중 시간 직접 설정

### 변경된 기능

- 고정된 25분 집중 시간 대신 1~120분 사이의 시간을 사용할 수 있다.
- `−`, `+` 버튼으로 집중 시간을 5분씩 조절할 수 있다.
- 숫자 입력란에서 원하는 시간을 직접 입력할 수 있다.
- 타이머가 실행 중일 때는 시간 설정을 잠가 진행 중인 세션이 바뀌지 않도록 했다.
- 설정한 집중 시간은 브라우저를 닫았다가 다시 열어도 유지된다.
- 변경한 시간으로 세션을 완료하면 실제 설정값이 오늘의 기록에 저장된다.

### 변경된 파일과 코드

#### `app/page.tsx`

- `focusDuration` 상태를 추가하고 기본값을 25분으로 지정했다.
- `changeFocusDuration` 함수에서 입력값을 1~120분 범위로 제한한다.
- `moru-focus-duration` 키를 사용해 설정값을 `localStorage`에 저장하고 불러온다.
- 집중 모드 전환, 초기화, 세션 완료 기록이 `focusDuration` 값을 사용하도록 변경했다.
- 사이드 패널에 `duration-card` 영역과 시간 감소·직접 입력·증가 컨트롤을 추가했다.

#### `app/globals.css`

- `duration-card`, `duration-control` 스타일을 추가했다.
- 실행 중 비활성화된 버튼과 입력란의 상태가 구분되도록 스타일을 지정했다.

## 확인 결과

- `npm run build`가 오류 없이 완료됐다.
- 로컬 개발 주소 `http://localhost:3000`에서 정상 응답을 확인했다.

## 데이터 저장 방식

현재 앱은 외부 API나 데이터베이스를 사용하지 않는다. 아래 값은 앱을 연 브라우저의 `localStorage`에만 저장된다.

- `moru-tasks`: 할 일 목록
- `moru-sessions`: 완료한 집중 세션
- `moru-focus-duration`: 사용자가 설정한 집중 시간

브라우저 데이터를 삭제하거나 다른 브라우저·기기를 사용하면 기존 기록이 나타나지 않는다.
